Common Modules for AWS Build Out, built upon a "Terraservices" framework for AWS,

This module defines core infrastructure components which can be referenced using custom environment scenarios.

Work in progress.
